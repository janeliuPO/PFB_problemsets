#!/usr/bin/env python3
import sys
#write down nubers from 1 through N - range
#pick a random number - pop it out - create new list

sequence = 'ATCAG' #define sequence
length = len(sequence)
print(length)

sub_dna = sequence.replace(randrange(),randrange())
print(sub_dna)

